import { Statement, CallExpression, ObjectExpression } from '@babel/types';

declare const filterMacro: (stmts: Statement[]) => CallExpression[];
declare const hasPropsOrEmits: (node: ObjectExpression) => boolean;

export { filterMacro, hasPropsOrEmits };
